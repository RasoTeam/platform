<table>
	<tbody>
		<tr>
			<th>Companies <%= link_to "A-Z", backoffice_company_path(@company, params.merge({order: "ASC"})) %> <%= link_to "Z-A", backoffice_company_path(@company, params.merge({order: "DESC"})) %></th>
		</tr>
		<% @companies.each do |company| %>
			<tr>
				<td><%= link_to company.name, backoffice_company_path(company) %></td>
			</tr>
		<% end %>
	</tbody>
</table>
<%= will_paginate @companies %>
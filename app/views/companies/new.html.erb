<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :tag %>
  <%= f.text_field :tag %>
  <%= f.submit %>
  <% @company.errors.full_messages.each do |msg| %>
    <p><%= msg %></p>
  <% end %>
<% end %>
